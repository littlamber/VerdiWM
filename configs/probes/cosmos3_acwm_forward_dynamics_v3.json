{
  "schema_version": 1,
  "artifact_type": "wmloop-probe-registry",
  "library_version": "cosmos3-acwm-forward-dynamics-v3",
  "probes": [
    {"id": "rollout_video_psnr", "role": "verdict", "report_field": "rollout_video_psnr", "description": "Paired GT rollout PSNR on the frozen DROID LeRobot window."},
    {"id": "rollout_video_l1", "role": "verdict", "report_field": "rollout_video_l1", "description": "Paired GT RGB L1 error on generated future frames; lower is better."},
    {"id": "final_frame_mae", "role": "verdict", "report_field": "final_frame_mae", "description": "Final predicted-frame MAE against the aligned frozen target."},
    {"id": "temporal_difference_mae", "role": "verdict", "report_field": "temporal_difference_mae", "description": "MAE between predicted and GT temporal frame differences."},
    {"id": "action_conditioning_sensitivity", "role": "diagnostic", "report_field": "action_conditioning_sensitivity", "description": "Response to reversible H2 action-conditioning doses."},
    {"id": "action_translation_sensitivity", "role": "diagnostic", "report_field": "action_translation_sensitivity", "description": "Response to reversible H2 doses on DROID position-delta columns while rotation and gripper remain fixed."},
    {"id": "action_dimension_anisotropy", "role": "diagnostic", "report_field": "action_dimension_anisotropy", "description": "Response to signed H2 doses that rebalance per-dimension temporal action contrast while preserving every dimension mean."},
    {"id": "action_dimension_interaction", "role": "diagnostic", "report_field": "action_dimension_interaction", "description": "Response to signed orthogonal H2 rotations across frozen DROID action-dimension pairs, preserving temporal means and centered energy."},
    {"id": "autoregressive_drift", "role": "diagnostic", "report_field": "autoregressive_drift", "description": "Error growth across chained 16-frame forward-dynamics chunks."}
  ]
}
