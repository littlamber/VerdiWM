{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "artifact_type",
    "candidate_id",
    "candidate",
    "model_family",
    "stage",
    "settlement_state",
    "verification_state",
    "verdict_ref",
    "contract_id",
    "contract_digest",
    "policy_digest",
    "outcome",
    "promotion_state",
    "metric_deltas",
    "blockers",
    "evidence_refs",
    "claim_boundary"
  ],
  "properties": {
    "schema_version": {"type": "integer", "enum": [1]},
    "artifact_type": {"enum": ["verdiwm-acwm-verified-evidence"]},
    "candidate_id": {"type": "string", "minLength": 1},
    "candidate": {"type": "object"},
    "model_family": {"enum": ["ctrl-world"]},
    "stage": {"enum": ["confirm"]},
    "settlement_state": {"enum": ["settled"]},
    "verification_state": {"enum": ["verified"]},
    "verdict_ref": {"type": "string", "pattern": "^cas://sha256/[0-9a-f]{64}$"},
    "contract_id": {"type": "string", "minLength": 1},
    "contract_digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "policy_digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "outcome": {"enum": ["selected_positive", "confirmed_positive", "rejected_harmful"]},
    "promotion_state": {"enum": ["target_selected", "verified_not_selected", "rejected"]},
    "metric_deltas": {"type": "object"},
    "blockers": {"type": "array", "items": {"type": "string"}},
    "evidence_refs": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string", "pattern": "^cas://sha256/[0-9a-f]{64}$"}
    },
    "claim_boundary": {"type": "string", "minLength": 1}
  }
}
