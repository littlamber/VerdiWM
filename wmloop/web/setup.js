const form=document.querySelector("#setup-form"),message=document.querySelector("#message");
form.addEventListener("submit",async event=>{event.preventDefault();message.textContent="正在检查并保存…";const data=Object.fromEntries(new FormData(form));const payload={model:data.model,data:data.data,goal:data.goal,target_metrics:data.target_metrics?data.target_metrics.split(/\s+/).filter(Boolean):[],force:false};try{const response=await fetch("/api/first-contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body.error||"保存失败");if(body.state!=="ready"){message.textContent=(body.missing||[]).map(item=>item.message).join(" ");return}window.location="/"}catch(error){message.textContent=error.message}});
