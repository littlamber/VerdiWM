# Security

Do not commit credentials, tokens, private endpoints, model weights, datasets,
or absolute host paths. Evidence records must contain semantic identifiers and
digests rather than local filesystem locations. Report suspected vulnerabilities
privately to the repository owner before public disclosure.
