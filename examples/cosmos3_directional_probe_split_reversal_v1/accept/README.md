# Cosmos3 Target-Local Fingerprint

This bundle contains `9` paired-dose measurements on the frozen `accept` split. Locality admission is `passed`.

The chart measures a target-local response to reversible `action_conditioning_scale` doses in `relative_action_input_scale` units. It is not by itself evidence of model improvement or cross-backbone transfer.
