# Cosmos3 Target-Local Fingerprint

This bundle contains `9` paired-dose measurements on the frozen `accept` split. Locality admission is `failed`.

The chart measures a target-local response to reversible `action_translation_scale` doses in `relative_translation_action_scale` units. It is not by itself evidence of model improvement or cross-backbone transfer.
