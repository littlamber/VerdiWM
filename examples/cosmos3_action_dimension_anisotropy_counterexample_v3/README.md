# Cosmos3 Target-Local Fingerprint

This bundle contains `9` paired-dose measurements on the frozen `dev` split. Locality admission is `failed`.

The chart measures a target-local response to reversible `action_dimension_anisotropy` doses in `relative_action_dimension_contrast_balance` units. It is not by itself evidence of model improvement or cross-backbone transfer.
