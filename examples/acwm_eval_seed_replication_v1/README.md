# ACWM Eval-Seed Replication

This bundle contains `12` settled official-gate receipts over `4` environment/primitive cells. `2` cells pass all frozen evaluation seeds.

The videos use `GT | baseline prediction | primitive prediction`. This bundle measures evaluation-randomness robustness of fixed checkpoints; independent training-seed replication remains separate work.
