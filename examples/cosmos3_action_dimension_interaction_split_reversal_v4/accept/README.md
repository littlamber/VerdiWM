# Cosmos3 Target-Local Fingerprint

This bundle contains `9` paired-dose measurements on the frozen `accept` split. Locality admission is `failed`.

The chart measures a target-local response to reversible `action_dimension_interaction` doses in `radians_action_dimension_coupling` units. It is not by itself evidence of model improvement or cross-backbone transfer.
