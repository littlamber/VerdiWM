# Cosmos3 Directional Probe Held-Out Settlement

This bundle compares the same frozen directional probe on dev and accept. The locality residuals are `0.0731` and `4.2338`. The normalized Jacobian alignment error is `0.0345` against the frozen `0.5000` threshold. The final settlement is therefore `settled_abstained`.

Abstention reasons: `accept_locality_admitted`. This is a certificate counterexample, not model-improvement evidence. The `accept/` subdirectory contains the paired dose-response tables and videos.
