# Cosmos3 Target-Local Fingerprint

This bundle contains `15` paired-dose measurements on the frozen `dev` split. Locality admission is `failed`.

The chart measures a target-local response to reversible `action_embedding_temporal_mix` doses in `temporal_action_input_mix` units. It is not by itself evidence of model improvement or cross-backbone transfer.
