# Cosmos3-Nano Paired-GT Dev Baseline

Complete frozen dev coverage: `3` receipts. Mean future-frame PSNR: `21.2759` dB.

The CSV and SVG are derived from self-contained, SHA-verified receipts. Videos are aligned `GT | Cosmos3-Nano prediction` views. This is baseline quality evidence, not a primitive or transfer result.
